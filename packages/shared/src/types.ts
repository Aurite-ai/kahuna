/**
 * Shared type definitions for Kahuna
 *
 * This file contains types that are shared between the web and api packages.
 * Add domain types, API request/response types, and other shared interfaces here.
 */

/**
 * Placeholder type - replace with actual domain types as the application develops
 */
export interface PlaceholderType {
  id: string;
  createdAt: Date;
}
